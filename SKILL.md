---
name: subtitle-align
description: สร้างซับไตเติล (.srt) ภาษาไทยที่เวลาตรงกับเสียงจริง แม้ย่อคำให้สั้นกว่าที่พูด โดยใช้ Gemini ถอดข้อความแล้ว forced-align กับเสียงด้วย Whisper. Use whenever the user wants to make subtitles or captions for a video, fix subtitles that drift out of sync in CapCut/Premiere/DaVinci, shorten subtitle text while keeping timing correct, or generate an SRT file. Also trigger on ทำซับ, ใส่ซับ, ซับไตเติล, ซับเหลื่อม, ซับไม่ตรง, ตัดคำซับ, .srt, SRT, caption, subtitle timing.
---

# Subtitle Align

ทำซับที่เวลาไม่เหลื่อม โดยแยก **ข้อความ** ออกจาก **เวลา**: Gemini ถอดคำ
(แม่นเรื่องภาษาไทย) ส่วนเวลามาจากการจับคู่ข้อความกับเสียงจริงทีละตัวอักษรด้วย
Whisper forced alignment พอผู้ใช้ย่อคำทีหลัง จึงค้นเวลาของคำที่เหลือกลับมาได้

## ตำแหน่ง script

```
~/.claude/skills/subtitle-align/
```

ถ้า clone ไว้ที่อื่น ให้ใช้ path นั้นแทนทุกคำสั่งด้านล่าง
เรียกด้วย `python3 <path>/align.py` ได้จากทุกโฟลเดอร์ — ไม่ต้อง cd เข้าไปในโฟลเดอร์ skill

## เช็คก่อนรันครั้งแรก

```bash
python3 -c "import stable_whisper, google.genai" && which ffmpeg
```

ถ้าพัง ให้ติดตั้งก่อน แล้วบอกผู้ใช้ว่ากำลังลงอะไร:

```bash
pip3 install -r ~/.claude/skills/subtitle-align/requirements.txt
brew install ffmpeg
```

`GEMINI_API_KEY` หาจาก env, `.env` ในโฟลเดอร์ปัจจุบัน, `.env` ข้าง ๆ script,
หรือ `~/.config/subtitle-align/.env` — ถ้าไม่เจอ script จะบอกเองว่าต้องใส่ที่ไหน
**ห้ามถามหา key จากผู้ใช้ในแชท** ให้บอกให้เขาใส่ในไฟล์เอง

## Flow (ต้องหยุดกลางทางเสมอ)

งานนี้แบ่งเป็นสองครึ่ง โดยมีขั้นที่ผู้ใช้ต้องตัดสินใจเองคั่นกลาง — **ห้ามรันรวดเดียวจบ**

**ครึ่งแรก — align**

```bash
python3 ~/.claude/skills/subtitle-align/align.py <วิดีโอ> --max-chars 24
```

- `--max-chars 24` สำหรับคลิปแนวตั้ง (Reels/TikTok), ปล่อยค่าเริ่มต้น 42 สำหรับจอนอน
- ตัดเฉพาะช่วง: `--from 12:30 --to 13:05` (เวลาใน SRT จะนับ 0 ที่จุดเริ่ม)
- คลิปยาวเกิน ~10 นาทีไม่เหมาะกับ pipeline นี้ (ไม่มีการแบ่ง chunk) — ให้ใช้
  skill `transcribe-by-gemini` ถอดคลิปเต็มเพื่อหาช่วงก่อน แล้วค่อย align เฉพาะช่วงนั้น
- ครั้งแรกจะโหลด Whisper model ~1.4GB ให้บอกผู้ใช้ล่วงหน้าว่าจะใช้เวลา
- alignment กินเวลาราวครึ่งหนึ่งของความยาวคลิป

**หยุดตรงนี้** แล้วบอกผู้ใช้ว่า:
1. ไฟล์ `work/<ชื่อ>/edit.txt` พร้อมให้ย่อคำแล้ว (บอก path เต็ม)
2. กติกา: ลบคำได้ / บรรทัดว่าง = ตัดก้อนนั้นทิ้ง / `|` = แยกเป็นสองซับ /
   ห้ามพิมพ์คำใหม่ที่ไม่มีในเสียง
3. รอให้เขาบอกว่าแก้เสร็จ

ถ้าผู้ใช้บอกว่าไม่อยากย่อคำเอง ให้ข้ามไปครึ่งหลังได้เลย — `edit.txt` ที่ align
สร้างไว้ใช้เป็นซับเต็มประโยคได้ทันที

**ครึ่งหลัง — สร้าง SRT**

```bash
python3 ~/.claude/skills/subtitle-align/make_srt.py <ชื่องาน> --min-dur 0.8
```

ถ้ามีคำเตือน "จับคู่กับเสียงไม่ได้ชัด" ให้อ่านว่าก้อนไหน แล้วบอกผู้ใช้ว่าก้อนนั้น
มีคำที่ไม่มีในเสียง ต้องแก้ให้ใช้คำเดิมแล้วรันซ้ำ

## ปัญหาที่เจอบ่อย

| อาการ | สาเหตุและทางแก้ |
|---|---|
| `CERTIFICATE_VERIFY_FAILED` ตอนโหลด model | `align.py` ตั้ง `SSL_CERT_FILE` จาก certifi ให้แล้ว ถ้ายังพังให้เช็คว่า `pip3 install certifi` แล้ว |
| ถอดผิดคำ / ตกคำ | แก้ `work/<ชื่อ>/transcript.txt` แล้วรัน `align.py` ซ้ำ (มันจะข้ามขั้นเรียก Gemini เพราะไฟล์มีอยู่แล้ว) |
| ซับก้อนยาวเกินไป | `--max-chars` เล็กลง แต่ตัดได้เฉพาะตรงรอยเว้นวรรค ถ้าคลิปพูดรัวไม่มีจังหวะหยุด ก้อนจะยาวกว่าที่ตั้งไว้ |
| ซับขึ้นเร็ว/ช้าไปทั้งไฟล์ | `--lead` / `--tail` ตอน `make_srt.py` |
| อยาก align ใหม่ทั้งหมด | ลบโฟลเดอร์ `work/<ชื่อ>/` แล้วรันใหม่ |

## กฎเหล็ก

**align กับเสียงชุดสุดท้ายที่คนดูจะได้ยินเสมอ** ถ้าผู้ใช้จะตัดต่อเพิ่มหลังทำซับ
(โดยเฉพาะ jump cut ตัดคำออกกลางคลิป) เวลาจะเลื่อนทั้งหมด ต้อง export คลิปที่ตัด
เสร็จแล้วมา align ใหม่ — เตือนเรื่องนี้ทุกครั้งที่ผู้ใช้พูดถึงการตัดต่อ

## ข้อจำกัด

- ทดสอบกับเสียงพูดไทยที่ค่อนข้างชัด ยังไม่ได้ทดสอบกับเสียงเพลงคลอดัง
  หรือคนพูดทับกันสองคน
- `--from` / `--to` ใช้ได้เฉพาะกรณีตัดหัวท้าย
- Whisper ตัด token ภาษาไทยเป็นตัวอักษรเดี่ยว การแบ่งก้อนจึงอาศัยการเว้นวรรค
  ที่ Gemini ใส่ตามจังหวะพูด
